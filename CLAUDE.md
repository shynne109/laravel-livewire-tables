# CLAUDE.md

This project's agent guidance lives in **[AGENT.md](./AGENT.md)** — read it for how to install, scaffold, configure, and build tables with `rappasoft/laravel-livewire-tables`.

Claude Code reads both files; the content is kept in `AGENT.md` to avoid duplication. Follow it as written.
